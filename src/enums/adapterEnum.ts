export enum PlatFormEnum {
  chrome = 1,
  firefox,
  web
}

/**
 * 右侧菜单常量
 */
export enum XyContextMenuItem {
  CAMERA = 'xy-摄像头',
  DRAW = 'xy-绘画'
}

/**
 * 消息传递
 */
export enum XyMessageType {
  // 选项卡更新
  TAB_UPDATE = 'tabUpdate',
  // 到相机页
  PAGE_CAMERA = 'cameraPage'
}
